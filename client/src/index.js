import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
// import Button from './Button';
// If you want to run the final button code, uncomment the line below and comment out the line above:
import Button from './Final-Button';

ReactDOM.render(
  <React.StrictMode>
    <Button />
  </React.StrictMode>,
  document.getElementById('root')
);
